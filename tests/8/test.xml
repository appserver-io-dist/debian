<?xml version="1.0"?>
<!DOCTYPE project>
<project name="appserver-io-dist/debian-8-tests" basedir="..">

    <!-- Read in tests property files -->
    <property file="${basedir}/tests/8/tests.properties"/>
    <property file="${basedir}/tests/8/tests.default.properties"/>
    <property file="${basedir}/tests/generic/tests.properties"/>
    <property file="${basedir}/tests/generic/tests.default.properties"/>

    <!-- ==================================================================== -->
    <!-- Import the tests/*.xml files                                           -->
    <!-- ==================================================================== -->
    <import file="system.xml"/>

</project>
